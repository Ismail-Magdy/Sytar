import 'package:flutter/material.dart';

class SytarApp extends StatelessWidget {
  const SytarApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(debugShowCheckedModeBanner: false);
  }
}
